| Status   | Date       | Decision Makers                    |
| -------- | ---------- | ---------------------------------- |
| proposed | 2026-04-30 | [Ezhkin-Kot], [nrydanov], [t3m8ch] |

[Ezhkin-Kot]: https://github.com/Ezhkin-Kot
[nrydanov]: https://github.com/nrydanov
[t3m8ch]: https://github.com/t3m8ch

# Механика редактирования курсов

## Проблематика

Необходимо реализовать механизм редактирования курса преподавателем. При этом нужно правильно обрабатывать ситуацию, когда два преподавателя одновременно пытаются редактировать один курс, так как иначе это приведёт к потере данных и race condition. Также стоит добавить возможность версионирования и отката сделанных изменений.

Как обеспечить удобный UX редактирования курсов, возможность отмены изменений и гарантированную целостность данных в БД, обрабатывать возможные проблемные ситуации, а также оставить удобную возможность для дальнейшего улучшения?

## Факторы, влияющие на решение

- Ни при каких условиях изменения одного пользователя не должны молча затираться другим.
- Пользователь должен знать, что курс уже кем-то редактируется, до того как потратит время на свои правки.
- Возможность пользователя потерять несохранённые изменения должна сводиться к минимуму.
- Не должно быть длительных блокировок в БД.
- Не должно быть огромных по объёму запросов.
- Студенты не должны видеть промежуточное состояние курса в процессе редактирования.
- Нужно где-то хранить предыдущие версии редакций курса, чтобы пользователь мог откатить свои изменения.
- Система должна не допускать зависших блокировок (проверять, точно ли курс ещё редактируется), но при этом корректно обрабатывать обрывы связи.

---

## Рассматриваемые решения

### 1. Оптимистичная блокировка, один запрос на обновление всего курса

Механизм, основанный на версии записи в основной базе данных без какого-либо предварительного резервирования доступа. При завершении редактирования фронтенд шлёт один запрос на обновление курса со всем его содержимым.

- При загрузке курса клиент получает текущий номер версии (например, `version: 1`). При сохранении изменений клиент отправляет эту версию обратно. Бэкенд выполняет запрос: `UPDATE courses SET ..., version = 2 WHERE id = <id> AND version = 1`.
- Если с момента загрузки версию успел обновить кто-то другой, запрос обновит 0 строк, и система вернет ошибку.

#### Плюсы решения

- Максимально простая реализация.

#### Минусы решения

- Плохой UX: пользователь узнает о конфликте только в момент сохранения, теряя всю проделанную работу.
- Отсутствие возможности отката изменений.

---

### 2. Мягкая блокировка с TTL в Redis, отдельные запросы

Мягкая блокировка в Redis. Изменения каждого блока приходят в виде отдельных небольших запросов.

- Блокировка создается в кэше с коротким временем жизни (TTL, например, 60 секунд). Пока пользователь держит вкладку открытой, фронтенд автоматически отправляет сигнал (heartbeat) каждые 30 секунд, продлевая аренду.
- Если пользователь свернёт вкладку или у него надолго пропадёт связь, блокировка сама исчезнет через минуту, освободив курс для редактирования.
- При каждом изменении отдельных частей курса фронтенд шлёт запрос только с этими изменениями.

#### Плюсы решения

- Предотвращает конфликты до начала работы, сразу переводя интерфейс других пользователей в Read-only.
- Механизм TTL (Time To Live) в Redis автоматически освобождает курс, если пользователь перестал проявлять активность или у него пропал интернет.
- Позволяет отображать в UI, кто именно сейчас работает над курсом.
- Сделанные изменения сразу сохраняются и не будут утеряны.

#### Минусы решения

- Существует риск race condition: если блокировка в кэше истечет из-за лага сети одновременно с тем, как пользователь A нажмет на сохранение, а пользователь B в этот же момент перехватит блокировку, данные A могут затереть данные B, и B будет работать с неактуальным состоянием курса.
- Отсутствие возможности отката изменений.
- Промежуточное состояние курса в момент редактирования сразу отображается у студентов.

---

### 3. Мягкая + оптимистичная блокировка в Postgres, отдельные запросы, снапшоты курсов

Комбинация оптимистичной блокировки для физической целостности данных и мягкой блокировки для управления доступом в интерфейсе. Обе блокировки хранятся в Postgres. Отдельные запросы при изменении каждого блока. Версионирование курсов по снапшотам: каждая новая редакция курса (новая версия) является отдельным снапшотом.

- Мягкая блокировка создаётся в Postgres в отдельной таблице и хранит время, до которого она действительна. Это время периодически продлевается по heartbeat. Но финальное сохранение изменений всё равно сопровождается проверкой `version`.
- При каждом изменении отдельных частей курса фронтенд шлёт запрос только с этими изменениями.
- Добавляется механика снапшотов:
  - объект таблицы `courses` хранит поле, указывающее на актуальный снапшот курса;
  - снапшот - курс с его содержимым на определённым момент времени;
  - при начале редактирования курса создаётся новый снапшот, над ним проводятся все изменения, затем, при завершении редактирования, этот снапшот курса становится в upstream, то есть то поле объекта в `courses` начинает указывать на него;
  - пользователь может откатить свои изменения на предыдущие снапшоты

#### Плюсы решения

- Сочетает лучший UX и максимальную надежность.
- Оптимистичная блокировка (`version` курса) служит страховкой на случай, когда мягкая блокировка истекла преждевременно.
- Не появляется лишней зависимости от Redis.
- Сделанные изменения сразу сохраняются и не будут утеряны.
- При этом студенты не видят редактируемую версию курса, так как это происходит в отдельном снапшоте.
- Появляется возможность отката изменений на этап до начала редактирования.

#### Минусы решения

- Объём изменений между снапшотами может быть достаточно большим (нельзя откатить только часть изменений).
- При частых изменениях курса его снапшоты могут в теории занимать много места в БД, так как они хранят все свои блоки, но на практике это вряд ли будет проблемой.

---

### 4. Мягкая + оптимистичная блокировка, отдельные запросы, снапшоты курсов, хранение промежуточных этапов редактирования

Всё вышеописанное, но в придачу с сохранением всей истории небольших изменений между снапшотами.

- Создаётся таблица в БД, хранящая последовательность атомарных изменений курса. Хранить это как полные снимки курса или его частей слишком ресурсозатратно и не эффективно, поэтому лучше сохранять изменения в виде диффа между этими промежуточными версиями редактируемого курса, либо в виде каких-то команд с производимыми действиями.
- Эти промежуточные изменения сохраняются только для последнего снапшота, либо для нескольких последних снапшотов.
- Каждое небольшое изменение элементов курса, сделанное пользователем в процессе редактирования, (изменение порядка блоков, их добавление, удаление, изменение содержимого) сохраняется в этой таблице, и пользователь может перемещаться по этим изменениям.
- Если пользователь откатился на некоторое число изменений назад и из этого состояния внёс новое изменение, то вся линия последующих изменений перезаписывается новым (как обычно в большинстве редакторов).
- Нужно определиться, насколько маленькие изменения стоит сохранять. Например, чтобы при редактировании содержимого блока с текстом можно было откатиться не просто до момента начала редактирования конкретно этого блока, а прям до определённых этапов процесса написания текста.

#### Плюсы решения

- Все плюсы решения, описанного выше.
- Пользователь может максимально удобно перемещаться по истории изменений.

#### Минусы решения

- Большая нагрузка на сеть и БД при слишком частых и мелких изменениях (но является ли это проблемой?).
- В варианте с отслеживанием изменений на уровне содержимого блоков (например текста) фронтенд должен уметь автоматически детектить эти промежуточные изменения и правильно отправлять их на бэкенд.
- Сложность реализации.

---

### 5. Снапшоты курсов с copy-on-write

Всё вышеописанное, но каждый из снапшотов курсов хранят не все его блоки, а только те, что были изменены в рамках соответствующей редакции курса.

- При создании снапшоты курса не хранят никаких блоков, и в процессе редактирования в снапшот добавляются только те блоки, которые были изменены.
- Затем эти снапшоты каким-то образом последовательно применяются к курсу, отражая его актуальное состояние.

#### Плюсы решения

- Снапшоты занимают меньше места в БД.

#### Минусы решения

- Дополнительные расходы на сборку всех снапшотов в один курс со всеми блоками.
- Необходимо как-то отдельно обрабатывать удаление блоков в снапшоте, так как простое отсутствие блока в случае copy-on-write будет восприниматься как отсутствие его изменений.

---

### 6. Одновременное совместное редактирование курса

Можно реализовать возможность одновременного редактирования одного курса несколькими пользователями. Для этого существует множество подходов, вот некоторые из рассмотренных:

- Блокировка на уровне блоков на всё время редактирования.
- Временная блокировка блоков только на время взаимодействия.
- Conflict-free Replicated Data Types (CRDT)

#### Плюсы решения

- Возможность редактировать курс одновременно несколькими пользователями.

#### Минусы решения

- **_ГИБЕЛЬ_**

---

## Принятое решение

**3. Мягкая + оптимистичная блокировка в Postgres, отдельные запросы, снапшоты курсов**

- Было принято решение просто блокировать возможность редактирования курса, если в этот момент он уже редактируется кем-то другим, так как ситуация одновременного редактирования одного курса несколькими пользователями крайне редкая, но при этом требует чрезмерно сложной в реализации логики работы, что на данный момент не стоит затраченных усилий.
- Мягкая блокировка позволяет сразу предотвращать на фронтенде начало редактирования курса кем-то другим, а проверка версии курса при сохранении гарантирует финальную целостность данных на уровне БД.
- Фронтенд отправляет запросы при изменении каждого отдельного блока, а также с определённым интервалом отправляет heartbeat для поддержания блокировки.
- При начале редактирования курса создаётся его новый снапшот, который пока не будет виден студентам, и все сделанные изменения производятся над ним.
- Когда редактирование завершается, блокировка снимается и курс начинает ссылаться на этот новый снапшот.
- Механизмы хранения промежуточных этапов редактирования снапшота и добавления блоков в снапшот по copy-on-write могут иметь смысл, но пока не являются приоритетными, поэтому можно реализовать уже в будущем при необходимости.

## Положительные последствия

- Появится механизм редактирования курсов.
- Конфликты предотвращаются на раннем этапе.
- Данные защищены версионированием даже при сбое кэша или сети.
- Благодаря снапшотам и отдельным запросам для блоков риски потери данных сводятся к минимуму.
- Редактирование отдельного снапшота позволяет скрывать от студентов ещё не завершённые изменения курса.
- Появляется возможность откатывать изменения курса к состоянию предыдущих снапшотов.
- Остаются возможности для дальнейшего улучшения этого механизма.

## Негативные последствия

- Фронтенд должен поддерживать фоновые запросы (heartbeat) во время редактирования.

## Детали реализации

### 1. Изменения в таблицах Postgres

- Добавляется таблица `course_snapshots`, в которой хранятся все снапшоты курсов:

  ```sql
  CREATE TYPE snapshot_status AS ENUM ('draft', 'published', 'stale');

  CREATE TABLE course_snapshots (
      id uuid PRIMARY KEY DEFAULT uuidv7(),
      course_id uuid NOT NULL REFERENCES courses(id),
      version integer NOT NULL,
      status snapshot_status NOT NULL DEFAULT 'draft',
      created_by uuid NOT NULL REFERENCES users(id),
      created_at timestamp NOT NULL
  );

  CREATE UNIQUE INDEX idx_course_snapshots_history ON course_snapshots(course_id, version) WHERE (status = 'published');
  ```

- Добавляется таблица `course_locks`, в которой хранятся блокировки курсов на время их редактирования:

  ```sql
  CREATE TABLE course_locks (
      course_id uuid PRIMARY KEY REFERENCES courses(id),
      user_id uuid NOT NULL REFERENCES users(id),
      session_id uuid NOT NULL,
      expires_at timestamp NOT NULL
  );
  ```

- Таблица `courses` хранит только метаданные курсов. В неё добавляются следующие поля:
  - `active_snapshot_id uuid REFERENCES course_snapshots(id)` - указывает на снапшот, который сейчас видят студенты.
  - `version integer NOT NULL DEFAULT 1` - используется для оптимистичной блокировки при завершении редактирования.

- Так как содержимое теперь привязано к конкретному снапшоту, то в таблицу `blocks` вносятся следующие изменения:
  - Поле `course_id` заменяется на `snapshot_id uuid NOT NULL REFERENCES course_snapshots(id)`.
  - Для оптимизации операций перемещения блоков при редактировании (чтобы при перемещении одного блока вверх не приходилось менять значения позиций у всех блоков ниже него) тип поля `position` заменяется на `varchar(64) NOT NULL` и используется лексикографический индекс для сортировки блоков. Добавляется функционал для работы с такими индексами.
  - Добавляется поле `deleted_at timestamp`. Если это поле не равно `null`, то блок считается удалённым. Таблица блоков становится append-only.

### 2. Управление пессимистичными блокировками

Поскольку одновременное редактирование исключено, доступ к черновику регулируется монопольной пессимистичной блокировкой курса в Postgres:

- При нажатии кнопки начала редактирования фронтенд делает запрос `POST /api/v1/courses/{course_id}/lock`, передавая в контексте `user_id` пользователя и `session_id` - ID его сессии в браузере. Бэкенд пытается установить блокировку для этого пользователя следующей транзакцией:
  1. Выполняется блокировка курса в Postgres на время транзакции, чтобы исключить race condition:

     ```sql
     SELECT 1 FROM courses WHERE id = :course_id FOR UPDATE;
     ```

  2. Проверяется, есть ли у этого пользователя роль преподавателя в курсе:

     ```sql
     SELECT is_active
     FROM course_members
     WHERE user_id = :user_id AND course_id = :course_id AND role = 'TEACHER';
     ```

     - Если прав нет - возвращается `403 Forbidden`.

  3. Проверяется, есть ли у этого пользователя активная блокировка курса:

     ```sql
     SELECT user_id, session_id, expires_at
     FROM course_locks
     WHERE course_id = :course_id;
     ```

     - Если блокировки нет, или если `expires_at < time.Now()` - захватываем блокировку следующим запросом:

       ```sql
       INSERT INTO course_locks (course_id, user_id, session_id, expires_at)
       VALUES (:course_id, :user_id, :session_id, :now + INTERVAL '60 seconds')
       ON CONFLICT (course_id) DO UPDATE
       SET user_id = EXCLUDED.user_id, session_id = EXCLUDED.session_id, expires_at = EXCLUDED.expires_at;
       ```

       После этого переходим к инициализации черновика (см. [3.1 Инициализация черновика](#1-инициализация-черновика)).

     - Если блокировка есть и `expires_at >= time.Now()`, но не совпадают `user_id` или `session_id` - значит блокировка уже удерживается кем-то другим, или этим же пользователем на другой вкладке/устройстве. Возвращаем `423 Locked` и данные пользователя, удерживающего блокировку.
     - Если блокировка есть, `expires_at >= time.Now()`, `user_id` и `session_id` соответствуют, то просто продлеваем блокировку и возвращаем `200 OK` и `draft_snapshot_id`.

- После успешной установки блокировки фронтенд каждые 30 секунд отправляет запрос `POST /api/v1/courses/{course_id}/heartbeat`. Бэкенд проверяет, что у пользователя есть активная блокировка и продлевает её:

  ```sql
  UPDATE course_locks
  SET expires_at = :now + INTERVAL '60 seconds'
  WHERE course_id = :course_id
    AND user_id = :user_id
    AND session_id = :session_id
    AND expires_at >= :now;
  ```

  - Если `RowsAffected == 1` - блокировка успешно продлена, возвращается `204 No Content`.
  - Если `RowsAffected == 0` - блокировка была потеряна, возвращается `423 Locked`.

- Если фронтенд при отправке heartbeat получил `423 Locked`, то он пробует ещё раз отправить запрос `POST /api/v1/courses/{course_id}/lock` чтобы попробовать восстановить утраченную блокировку, если никто не успел её перехватить.

### 3. Процесс редактирования

#### 1. Инициализация черновика

Если после отправки `/lock` запроса произошёл захват блокировки, то бэкенд начинает инициализацию черновика. Сначала проверяется, не осталось ли у этого пользователя незавершённой сессии редактирования, которую можно восстановить:

- Бэкенд пробует найти в БД незавершённый черновик, созданный этим пользователем:

  ```sql
  SELECT id, version FROM course_snapshots
  WHERE course_id = :course_id AND created_by = :user_id AND status = 'draft'
  LIMIT 1;
  ```

  - Если такой черновик найден, то проверяется актуальность этого черновика, то есть что его `version` на 1 больше текущей версии курса.
    - Если черновик актуален, значит никто до этого пользователя не успел внести никаких своих изменений и публикация этого черновика не вызовет конфликтов. Восстанавливается блокировка для этого пользователя, фронтенду возвращается статус `200 OK`, а в теле ответа передаётся `init_type = restored` - сообщение, что был восстановлен ещё актуальный незавершённый черновик, и `draft_snapshot_id` - ID незавершённого снапшота этого пользователя, в котором он может продолжить свои изменения.
    - Если версия черновика устарела, значит он не может быть опубликован. Фронтенду возвращается статус `200 OK`, а в теле передаётся `init_type = stale_conflict` - сообщение, что найден незавершённый черновик, но он устарел, и `draft_snapshot_id` - ID устаревшего снапшота, чтобы пользователь мог вручную скопировать себе из него нужные изменения и перенести в новый снапшот. Фронтенд при получении `init_type = stale_conflict` выводит предупреждение, что черновик не может быть опубликован, и предлагает пересоздать его с нуля. Кнопка пересоздания снапшота просто сначала отправляет запрос `POST /api/v1/courses/{course_id}/cancel_edit` (см. [3.5. Отмена изменений](#5-отмена-изменений-и-механизм-восстановления-незавершенной-сессии)), а потом `POST /api/v1/courses/{course_id}/lock`.
  - Если снапшот этого пользователя со статусом `draft` не был найден, то бэкенд создаёт новый черновой снапшот. Для этого выполняется следующая транзакция в Postgres:
    1. Создаётся новая запись в `course_snapshots` с значением `version`, на 1 больше текущей версии курса, и `status = 'draft'`.
    2. Копируются все блоки из текущего `active_snapshot_id` в только что созданный снапшот (`INSERT INTO blocks (...) SELECT ... WHERE snapshot_id = active_snapshot_id AND deleted_at IS NULL`).
    3. Фронтенду возвращается статус `200 OK`, а в теле передаётся `init_type = new` - сообщение, что был создан новый черновик, и `draft_snapshot_id` - ID нового снапшота. Студенты этого снапшота не видят, так как поле `active_snapshot_id` в таблице `courses` всё ещё указывает на старый снапшот.

#### 2. Атомарные изменения блоков в процессе редактирования

Любое действие преподавателя на фронтенде отправляет отдельный запрос, который применяется к новому черновому снапшоту:

- **Добавление блока:** При запросе `POST /api/v1/blocks` с телом `{ "block_type": "text", "data": "jsonb", "snapshot_id": "UUID", "after_block_id": "UUID" }` создается новый блок в снапшоте. На бэкенде ему присваивается `position` такой, чтобы он встал сразу после блока `after_block_id`.
- **Редактирование содержимого:** Запрос `PATCH /api/v1/blocks/{block_id}` позволяет обновить поле `data` - содержимое конкретного блока, а также `block_type`.
- **Перемещение (Drag & Drop):** Фронтенд отправляет запрос `PATCH /api/v1/blocks/{block_id}/move` с телом `{ "after_block_id": "UUID" }`. Бэкенд вычисляет `position` переданного блока `after_block_id`, после которого нужно поставить перемещаемый, затем ищет блок, который в данный момент стоит после `after_block_id`, вычисляет его `position`, генерирует промежуточный между ними `position` и обновляет это значение у перемещённого блока.
- **Удаление:** При запросе `DELETE /api/v1/blocks/{block_id}` у указанного блока ставится `deleted_at = :now`.

- При каждой из этих операций попутно проверяется наличие активной блокировки курса за этим пользователем. Если блокировка утеряна (например, из-за таймаута связи), то эти запросы возвращают `423 Locked` и фронтенд может попробовать вернуть блокировку, отправив запрос на `/lock`.

#### 3. Перемещение по таймлайну снапшотов курса

Так как каждый сеанс редактирования курса сохраняется в БД в виде отдельного снапшота, то можно дать преподавателю возможность откатывать состояние курса к нужному снапшоту.

- Запрос `GET /api/v1/courses/{course_id}/snapshots` возвращает список всех снапшотов курса из актуального таймлайна, а также ID снапшота, на который указывает курс в данный момент.
  - На бэкенде этот список получается из Postgres благодаря индексу `idx_course_snapshots_history` следующим запросом:

    ```sql
    SELECT s.id, s.version, s.created_at, u.name, u.first_name, u.last_name, u.patronymic
    FROM course_snapshots s
    JOIN users u ON s.created_by = u.id
    WHERE s.course_id = :course_id AND s.status = 'published'
    ORDER BY s.version DESC;
    ```

- Запрос `POST /api/v1/courses/{course_id}/switch_snapshot` с телом `{ "target_snapshot_id": "UUID" }` позволяет откатить текущий черновик к состоянию указанного снапшота.
  1. Проверяется наличие активной блокировки за этим пользователем.
  2. Находится целевой снапшот по его `id` и проверяет, что он принадлежит данному курсу и имеет статус `published`.
  3. У всех блоков в черновике устанавливается `deleted_at = :now`, и затем в него копируются все блоки целевого снапшота.

#### 4. Завершение редактирования и публикация нового снапшота

Когда преподаватель нажимает на завершение редактирования, фронтенд отправляет запрос `POST /api/v1/courses/{course_id}/publish`, передавая в теле `id` чернового снапшота, и выполняется заключительная транзакция в Postgres:

1. Выполняется попытка замены `active_snapshot_id` курса на `id` нашего снапшота:

   ```sql
   UPDATE courses
   SET active_snapshot_id = :new_snapshot_id, version = version + 1
   WHERE id = :course_id AND version = :expected_version;
   ```

   Если `RowsAffected == 0`, транзакция откатывается, возвращается `409 Conflict` (сигнал о том, что версия курса изменилась). Если же версия курса правильная, то `active_snapshot_id` курса устанавливается равным `id` нашего снапшота.

2. Затем у нашего снапшота значение `status` устанавливается в `published`:

   ```sql
   UPDATE course_snapshots
   SET status = 'published'
   WHERE id = :new_snapshot_id;
   ```

3. При успешном обновлении бэкенд удаляет из Postgres блокировку для этого пользователя. Теперь обновленный курс со всеми новыми блоками мгновенно становится доступен студентам.

#### 5. Отмена изменений и механизм восстановления незавершенной сессии

- Если пользователь нажимает кнопку отмены всех незавершённых изменений, фронтенд отправляет запрос `POST /api/v1/courses/{course_id}/cancel_edit`, и бэкенд удаляет из Postgres блокировку этого пользователя, ставит у чернового снапшота `status = stale`, после чего у всех блоков этого снапшота ставит `deleted_at = :now`.
